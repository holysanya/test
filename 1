class PhoneContact:
    def __init__(self, name, contactNumber, note):
        self.name = name
        self.contactNumber = contactNumber
        self.note = note

    def display(self):
        print(self.name, self.contactNumber, self.note)
